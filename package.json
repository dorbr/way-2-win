{
    "name": "the-way-2-win-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npx concurrently \"npm run dev --prefix services\" \"npm run dev --prefix app\"",
        "dev:services": "npm run dev --prefix services",
        "dev:app": "npm run dev --prefix app",
        "install:all": "npm install && npm install --prefix services && npm install --prefix app",
        "build": "npm run build --prefix app && npm run build --prefix services",
        "start": "npm run start:prod --prefix services"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}